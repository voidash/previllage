# --- Rust ---
/target/
**/*.rs.bk
Cargo.lock.bak

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venvs/
venv/
.pytest_cache/
.ruff_cache/

# --- macOS ---
.DS_Store
._*

# --- Editors ---
.idea/
.vscode/
*.swp
*.swo

# --- Secrets / credentials ---
.env
.env.*
!.env.example
*.pem
*.key
*_rsa
*_rsa.pub
*.token
hf_token*
**/huggingface_token*
.admin_password
.whatsapp_bridge_token
whatsapp-auth/
**/whatsapp-auth/

# --- Large binary/generated artifacts ---
node_modules/
**/node_modules/
dist/
**/dist/
build/
coverage/
tmp/
*.sqlite
*.db

# Downloaded gov PDFs — regenerate via scripts/download_batch.py
survey/cdn_batch/
survey/ocr_output/

# CPT/SFT corpus assembly outputs — large, regenerable from source scripts.
# Keep the small public registries needed by the resolver and source-router.
corpora/*
!corpora/nepal_districts.jsonl
!corpora/sources_tiered.jsonl

# Pipeline outputs — regenerable from source PDFs + code
survey/corpus_chunks.jsonl
survey/bm25_index.json
survey/classification_batch.json
survey/classify.err

# Eval outputs (T9 on k2; local shouldn't have these but just in case)
eval/
*.log

# HF caches — on T9, not in repo
hf_cache/
**/hf_cache/

# MLX / model weights — on T9
checkpoints/
*.safetensors
*.gguf

# Demo capture artifacts kept outside the share branch.
/*.png
/*.jpg
/*.jpeg
/*.webp
/*.mp4
/video_*/
/whatsapp-demo-local.png

# Raw video/audio capture and render workspaces. Curated public screenshots live
# under assets/ and are intentionally not ignored.
footage/
audio/
renders/
analysis/transcripts/
remotion/public/project/

# --- Local overrides ---
*.local
local_settings.*

# Submission/research scratch captures not intended for GitHub.
/kaggle_*_innertext.txt
/gemma_4_good_overview_innertext.txt
/previllage_kaggle_under_1500_textedit.txt
/cited
/future
/officer
/reviewed

# --- Session-local agent / launch artifacts (may contain instance IDs, tokens) ---
.aws-launches/
.claude/
.playwright-mcp/
